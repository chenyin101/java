TASK1 
1  
  JDK是完整编辑和运行Java的软件和运行所需虚拟环境的部分，一般是编码端安装
    JRE有运行Java的软件和运行所需虚拟环境的部分，一般是使用端安装
    JVM为java运行的虚拟环境
     JDK包含JRE,JRE包含JVM
2
  java是高级语言，用Java直接写的代码计算机无法识别，需要用到JDK中的javac转化汇编语言 class格式，再用java在jvm上才能运行
3
  JDK26
4
  在库图片里
TASK2
1
   先新建了JAVA_HOME的系统变量 又在Path中新建了%JAVA_HOME%\bin的环境变量
    JAVA_HOME可以告诉系统和其他软件jdk的位置可以避免path中很长的路径
    path中%JAVA_HOME%\bin的环境变量，可以在任意文件中运行javac和Java
2
  path可以进行跨文件调用工具吗，windows默认只能在当前文件夹里调用工具
  在path中存下jdk路径就可以直接使用java和javac
3
在库的图片
4
我先编写了helloworld.java的文件，用javac运行helloworld.java的文件出现了helloworld.class，又用java运行helloworld.class
出现了helloworld的字符 
helloworld.java为人能看懂的代码
helloworld.class是机器能看懂的汇编语言
javac把人能看懂的高级语言转化为机器能看懂的class文件
java把class文件放在jvm上运行
TASK3
1
 七部分
 1：包packeage 点出这个类在哪个文件夹里
 2：import导入  把Print导入后，可以省去com.Example.tool，后面可以直接调用它
 3：公共public 让被修饰的东西，谁都可以编辑
 4：主main 程序切入口 jvm从这开始运行
 5：class  辅助
 6：test调用 调用Test中的test
 7：print输出 把结果进行输出
2
   package 叫“包”，我把他理解成 文件夹
   使用package包名和目录对应，方便查找 工具就找tool
   方便管理代码
3
    import 的作用是：导入其他包里的类，让我不用写完整类名。
4
     java 程序启动时，JVM 会去找一个固定格式 public static void main(String[] args)
TASK4
看库里的图片

 

